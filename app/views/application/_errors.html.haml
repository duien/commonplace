- if model.errors.any?
  #error_explanation
    %h2= "#{pluralize(model.errors.count, "error")} prohibited this #{model.model_name.singular} from being saved:"
    %ul
      - model.errors.full_messages.each do |msg|
        %li= msg
